# 开发状态与验证记录

更新日期：2026-09-18

## 当前实现

实现已建立文档定义的分层 C++20 工程，贯通 M0 灰港样例，完成 M1—M3 与 M4 的本地/Mock 代码基线，并进入 M5 Windows 闭环阶段：

- `domain`：灰港人物知识、唯一印章、秘密传播与规则不变量。
- `engine`：无需 API Key 的三回合 `MockProvider`、逐角色可见上下文和生产会话调度。
- `storage_sqlite`：SQLite C API RAII、迁移、WAL、短事务、命令去重、提交链、重启恢复和分支隔离。
- `application`：打开、单步/连续推演、暂停/恢复/取消、导演介入、创建/切换分支用例。
- `viewmodels` / `ui/qml`：后台执行数据库和 Mock 调用，GUI 线程只更新界面状态。
- `contracts`：命令、受限人物意图、人物卡和虚构时间的 draft-2020-12 Schema 基线及合法/非法夹具。
- `world_entity`：通用世界条目的稳定 ID、修订历史、软删除、别名、标签、扩展属性和乐观并发。
- `source_document`：TXT/Markdown UTF-8 标准化、原文与标准化资产、章节索引及 Unicode 码点证据区间。
- `evidence_reference`：字段级证据绑定稳定条目、来源、码点区间、引文哈希与来源性质；可从记录跳回原文选择区。
- `character_blueprint`：核心身份、可适配能力、装备、经历和作者私密说明独立字段；历史版本不可变。
- `package`：依赖无关 JSON 与受限 ZIP 实现；世界包、人物包支持摘要校验及单事务导入。
- `provider_connection`：提供商元数据修订、Windows Credential Manager 凭据边界、后台端点验证与固定脱敏错误。
- `providers`：OpenAI Responses、OpenAI-compatible Chat Completions、Anthropic Messages、Gemini GenerateContent 的独立请求/响应转换、usage 与终止原因分型；凭据不进入请求 DTO/日志。
- 原生界面：可切换推演室、世界资料、来源与章节、人物卡、包与备份、模型连接、工作区、任务中心、校对中心和世界视图十个页面。

## W0 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| W0-01 | 完成 | Windows 11；Qt 6.11.1 MinGW 13.1；CMake 4.2.1；Ninja 1.13.2；SQLite 3 静态库 |
| W0-02 | 进行中 | CMake 分层目标及 Qt Quick 界面已建立；仍需发行环境复测 |
| W0-03 | 进行中 | 四类最小 Schema 与合法/非法命令夹具；完整生产校验器待 M1 锁定 |
| W0-04 | 进行中 | SQLite RAII、迁移、事务、保存重启、命令去重及在线一致备份/恢复已实现；存储满故障注入待做 |
| W0-05 | 完成 | 固定灰港状态；非法转移、秘密越界测试覆盖 |
| W0-06 | 进行中 | 异步固定响应可用；错误、分片、取消矩阵待补 |
| W0-07 | 完成 | 三回合每回合只产生一次事务提交 |
| W0-08 | 进行中 | 重启 hash 一致及兄弟分支隔离已测试；指定崩溃点注入待补 |
| W0-09 | 进行中 | 单步、暂停、状态差异、分支 Qt Quick 页面已实现；自动化 UI 测试待补 |
| W0-10 | 进行中 | SSE 增量解析已通过任意字节分片、中文/emoji、CRLF、多 data 行、截断和缓冲上限测试；Qt HTTP 传输与真实端点待做 |
| W0-11 | 未开始 | 静态/动态检测器与取消竞态故障用例尚未实施 |
| W0-12 | 进行中 | `windeployqt` 部署目录已生成，清除插件环境变量后可独立启动并截图；外部干净 Windows 环境尚未实测 |
| W0-13 | 待条件 | Android/macOS/Linux 探针缺少当前平台环境，尚未验收 |
| W0-14 | 进行中 | `core-dev` 无 Qt GUI/Quick 配置、构建和测试通过；HTTP 测试入口尚未实施 |

未完成的 M0 项目及 M1—M9 不得视为已验收。

## M1 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| M1-01 | 完成 | QStandardPaths 平台目录、创建/打开 SQLite 工作区、最近项目（最多 20 个）与进程级安全切换已实现；`--workspace` 可直接打开指定项目 |
| M1-02 | 完成 | 通用条目 CRUD、稳定 ID、类型、别名、标签、说明、扩展 JSON；Qt 页面可直接编辑 |
| M1-03 | 完成 | 人物、势力、地点、物品、规则、事件通用条目可编辑；可移植人物卡独立版本化，旧版本保留且过期编辑冲突，Qt 页面可编辑 |
| M1-04 | 完成 | TXT/Markdown 原文资产、章节、标准化文本、字段级证据关联与 UTF-16/Unicode 码点双向跳转已实现；引文保存前从原文重取并校验哈希 |
| M1-05 | 完成 | 当前 Schema v17 可重复迁移、短事务、`expected_revision` 冲突及按规范化数据库路径共享的进程内单写入协调；12 连接并发写回归通过 |
| M1-06 | 完成 | 中文子串搜索、仓储 offset/limit、25 条 UI 翻页、空状态，以及按工作区和实体修订隔离的 650 ms 自动草稿恢复/丢弃已实现 |
| M1-07 | 完成 | 世界包/人物包 JSON/ZIP；完整工作区备份使用在线 SQLite 快照并携带资产 SHA-256 清单，恢复到新目录；摘要篡改无半恢复，凭据明文扫描为零 |
| M1-08 | 完成 | 提供商 CRUD、幂等命令与乐观锁；API Key 仅存 Windows Credential Manager，SQLite/WAL 泄漏回归与原生凭据往返通过；后台 `/models` 鉴权探测有 10 秒超时，错误只显示固定分类且不记录请求头 |

## M1 验收结论

M1 已完成并通过当前自动化退出基线：AC-01（无 Key 编辑与重启）、AC-02（稳定 ID 的可逆实体合并/拆分及证据引用修复）、AC-03（修订冲突）、AC-04（世界包往返）、AC-16（数据库与资产一体备份恢复且排除凭据）、AC-17（恶意 ZIP 与原子回滚）。外部干净 Windows 的 AC-37 仍属于发行验证，不并入 M1 完成声明。

## M2 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| M2-01 | 完成 | TXT/Markdown 支持 UTF-8、UTF-16 LE/BE、GB18030 检测，保留原始/标准化资产；章节预览可按 Unicode 码点手工校正，过期修订冲突 |
| M2-02 | 完成 | 持久化任务、段落边界分块、重叠、进度、取消、失败/unknown 恢复与单步骤重试；Qt 任务中心后台执行 |
| M2-03 | 完成 | `candidate-v1` 实体/事件/关系/规则协议与 `extract-v1` 提示词版本化；严格 JSON 大小、深度、数量和字段验证 |
| M2-04 | 完成 | 每个候选从不可变标准化原文按码点重取引文并校验 SHA-256；伪造引文整步原子拒绝 |
| M2-05 | 完成 | 别名参与中文检索；同名条目允许显式并存；稳定 ID 的实体合并/拆分会原子重写并恢复证据引用，映射历史保留 |
| M2-06 | 完成 | 校对中心支持修改、保留候选、标记冲突、拒绝、接受；审核修订、世界条目和证据引用同事务提交，乐观锁与命令重放测试通过 |
| M2-07 | 完成 | 缓存键固定块 hash、实体索引 hash、Schema、提示词、提供商/模型参数；新版本复用未变块并平移证据，变化块和实体索引变化会失效；失败块独立重试 |
| M2-08 | 完成 | SQLite 先按世界、故事时间和 `public/author/restricted+actor grant` 过滤，再做本地中文名称、别名、标签、说明召回；秘密不会先发给外部重排器 |
| M2-09 | 完成 | 建任务时给出保守输入 token 估计、单次输出上限和调用硬上限；领取步骤事务消耗额度，失败/unknown 不返还；价格未知明确显示而非 0；本地抽样只报告可验证的证据定位率 |

## M2 验收结论

M2 的本地与 Mock 退出基线已通过：AC-05（中文/emoji/CRLF 码点定位）、AC-06（故事时间过滤，`in_text_claim` 保留为 claim 而非世界真相，未知不强填）、AC-07（检查点、unknown 与缓存恢复）、AC-08（提示词要求未知字段保持缺失，推断保留 hypothesis 标记）、AC-18（来源中的“发送 API Key”仅作为不可信文本，提取层没有凭据/工具权限）。

真实模型的提取精确率、召回率和实际厂商费用尚未实测，因此不宣称达到文档中的 90%/85% 质量目标；界面和抽样报告会明确显示“模型质量尚未验证”。这项限制不影响本地协议、证据、安全边界和可恢复队列的完成状态。

## M3 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| M3-01 | 完成 | 发布世界版本会冻结排序后的 `entity_id + entity_revision + retrieval_scope_revision` 成员索引与内容 hash；父版本链不可变，后续条目/范围修改不改写旧版本 |
| M3-02 | 完成 | 时间事件分别保存可空故事时间、叙述顺序和相对时间；倒叙回归证明两种排序互不冒充，未知时间保留在独立区域 |
| M3-03 | 完成 | 事件保存前置、原因、结果边及 `fact/claim/hypothesis/future_candidate`；按时间点查看会排除未来候选而保留未知项 |
| M3-04 | 完成 | A→B 与 B→A 独立保存维度、强度、有效时间、证据/假设和可见范围；角色授权过滤回归通过 |
| M3-05 | 完成 | 地点树、底图资产/可空像素坐标、拓扑路线和可空行程时间分离；层级环被拒绝，证据与假设分别着色，不自动生成真实坐标 |
| M3-06 | 完成 | 历史快照固定世界版本与范围修订，按故事时间排除未来状态；`story_time_unknown` 条目进入待补全清单，快照命令重放 hash 一致 |
| M3-07 | 完成 | 人物卡版本可生成世界人物实例；能力/装备适配、知识策略与冲突报告持久化；同卡投放两个世界的记忆隔离且不回写通用卡 |
| M3-08 | 完成 | 分支根固定世界版本、快照、人物卡版本和实例修订；支持 `original_constrained`、`branching`、`sandbox`，已绑定根禁止原地改绑 |

## M3 验收结论

M3 退出基线 AC-09、10、11、12 已由核心回归覆盖：早期快照排除未来事件并提示未知项；同一林舟卡在两个世界产生独立实例/记忆；定向关系按时间和人物权限过滤；地图未知坐标保持未知、假设标点不冒充证据。原生“世界视图”页面提供版本/快照/人物入场、时间事件、定向关系和地图路线四个工作区；所有数据库操作在后台执行，首次多 ViewModel 并发迁移也纳入工作区单写入协调。

## M4 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| M4-01 | 完成 | `simulation_session/turn/provider_call` 状态持久化；启动把已发出但无终结记录的调用恢复为 `unknown` 并安全暂停 |
| M4-02 | 完成（灰港域） | 许澄与沈棠分别构建可见上下文；沈棠请求中不含封门秘密，检查印章只增加本人知识，合法私下披露后才传播 |
| M4-03 | 完成 | `intent.schema.json` 严格限制嵌套字段、受众和领域操作；合法/非法夹具及 C++ 身份、输入提交、目标语义校验通过 |
| M4-04 | 完成（灰港域） | 检查、秘密披露、唯一印章转移均转换为有限领域命令；未知操作、未知目标、无知识披露和无同意转移被拒绝 |
| M4-05 | 完成 | 回合预留、分支头复核、状态快照、知识变化、usage 和会话检查点同事务提交；逻辑命令、导演命令和回调重放恰好一次 |
| M4-06 | 完成 | 事实提交后回合进入 `narration_pending`，叙事单独完成；叙事失败不撤销事实，Qt 页面区分草稿/已提交状态 |
| M4-07 | 代码完成 / 真实未验收 | 人物绑定持久化；四类原生协议分别转换结构化输出并解析完成、拒绝、截断、usage；真实 HTTP 调用及模型能力矩阵仍需有效凭据验收 |
| M4-08 | 完成 | 事务预留调用额度，完成后结算实际 token；中断/超时未知调用单列且不自动返还，存在 unknown 时禁止自动恢复 |
| M4-09 | 完成（Mock） | 单步、连续上限、暂停、恢复、取消、无进展、场景完成、硬预算停止；导演介入独立审计且不伪装为模型调用 |
| M4-10 | Mock 完成 / 真实未验收 | 短场景、重放、分支提交、提交前后/叙事阶段恢复均有回归；未配置两家真实厂商凭据，因此真实同场景冒烟仍待条件 |

## M4 验收结论

本地与 Mock 基线覆盖 AC-14、15、19、20、21、22、23，并复用 M3 的分支根与历史模式覆盖 AC-24 的数据边界。特别地，暂停期间返回的模型结果只结算 usage 并保存为 `needs_review`，不会推进分支；显式恢复后会重新校验原输入提交且不产生第二次模型调用。

AC-13 要求至少两家真实模型参与同一场景，当前机器未提供相应凭据，故 M4 只能按路线文档允许的 Mock 验证版本记录，不能宣称真实厂商退出条件已经通过。当前生产状态结构仍以灰港有限领域作为首个裁判适配样例；扩展到任意世界规则属于后续领域适配工作，不能把协议转换测试误称为通用剧情质量验证。

## M5 状态

| ID | 状态 | 证据 / 限制 |
|---|---|---|
| M5-01 | 完成（本地） | 分支比较可重建共同祖先与各自提交链，并汇总状态、知识、信任、唯一物品及调用/token/unknown 差异；回归覆盖兄弟分支隔离 |
| M5-02 | 完成（本地） | 选定分支结果会以 `simulation_result`、`candidate` 溯源事件发布为新的不可变世界版本；命令重放幂等，失败后可继续完成第二阶段 |
| M5-03 | 完成（本地） | 仅导出已提交回合的 Markdown、JSON 或纯文本，包含世界/分支/状态/usage 摘要并可选技术标识；草稿和 `needs_review` 不进入正文 |
| M5-04 | 进行中 | 任务中心、备份恢复和脱敏诊断导出可用；诊断不含正文、本地路径、端点或凭据。统一错误恢复页与端到端交互复测仍待完成 |
| M5-05 | 未开始 | 尚未制作独立演示世界和首次使用引导流程 |
| M5-06 | 进行中 | 10,000 条实体单事务导入及首/中/末分页查询压力测试通过（本机 1.07 秒）；UI 长时运行、内存上限和取消竞态仍待专项验收 |
| M5-07 | 进行中 | `windeployqt` 便携目录、发行清单和 ZIP 已生成，并在脱离开发 PATH 后实际启动及截图；NSIS 脚本保留应用数据，但当前机器未安装 NSIS，干净外部 Windows、升级、卸载及签名仍未验收 |
| M5-08 | 进行中 | Windows UI 与无界面核心构建的 `core_tests`、`stress_tests`、`contract_tests` 均通过；真实提供商、存储满、动态检测器和用户验收仍待条件 |

## M5 当前验收结论

分支比较、结果采纳、导出、脱敏诊断、10,000 条实体压力基线与便携发行烟测已经形成可运行的本地实现。2026-09-18 在 Windows UI 与 `core-dev` 两套预设中各通过 3/3 测试；便携包在不向进程 PATH 添加 Qt/MinGW 开发目录的条件下退出码为 0 并生成界面截图。

M5 尚未达到路线文档的完整退出条件：没有外部干净 Windows 安装机、代码签名证书、两家真实模型凭据与真实用户验收，NSIS 安装器也尚未在本机生成。因此当前仍标记为进行中，不能将便携目录烟测等同于 AC-37 完整通过，也不能宣称 Windows 0.3-beta 已发布。
